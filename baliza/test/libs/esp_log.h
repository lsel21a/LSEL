#ifndef _MOCK_ESP_LOG_H
#define _MOCK_ESP_LOG_H

#define ESP_LOGE(...) do {} while (0)
#define ESP_LOGW(...) do {} while (0)
#define ESP_LOGI(...) do {} while (0)
#define ESP_LOGD(...) do {} while (0)
#define ESP_LOGV(...) do {} while (0)

#endif /* _MOCK_ESP_LOG_H */
